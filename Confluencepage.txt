Hi Shiva, the ACLP_DOCUMENT_METADATA table has been created based on the discussed schema.
A few points to consider while preparing the JSON-to-DB insertion script:
WORKBOOK_NAME should be treated as the source document/workbook identifier.
DOCUMENT_STATUS should capture the processing result, such as available, missing_sheet, missing_column, or unreadable. If the Engagement ID validation fails, please use the agreed status/error handling accordingly.
ENGAGEMENT_ID should be populated whenever it is available in the source JSON.
SUPPLIER_NAME / Third-party information should be populated from the JSON where available. Where only Engagement ID is available, the supplier/third-party details can be resolved using the existing Engagement/Third Party/Contract relationships as discussed.
Please retain the SHEET_NAME, HEADER_ROW, and MATCHING_COLUMNS information from the JSON because this is important for traceability and future reprocessing/debugging.
RUN_BATCH_ID should be the same UUID for all workbooks processed in a single execution/batch.
GENERATED_AT should come from the JSON generation timestamp, while CREATED_AT will be maintained by the database.
The table has a uniqueness rule on (RUN_BATCH_ID, WORKBOOK_NAME), so the same workbook should not be inserted twice within the same batch.
The detailed validation/business logic can remain in the existing Python processing flow; the DB script should mainly persist the final JSON output in the agreed structured format.
One thing I would clarify with Shiva
There is one important point I would confirm before he finalizes the insertion script:
What exactly should happen when the same Engagement ID appears multiple times in the JSON?
Your sample already has this situation:
ENG-2461151 → appears in multiple rows
ENG-449583 → appears in multiple rows
I would not tell him to deduplicate those records automatically. Those may represent different source rows and are useful for traceability.
So I would add one final question:
One clarification: if the same Engagement ID appears multiple times for the same workbook because it occurs in multiple source rows, should we retain each source-row-level record rather than deduplicating it? I assume we should retain them for traceability unless you prefer otherwise.
That is the main point I'd get confirmation on before Shiva writes the final loader.
Also, don't ask Shiva to worry about creating the table or SQLAlchemy model—that's already our side. His responsibility is essentially JSON → validation/business rules → ACLP_DOCUMENT_METADATA insertion.