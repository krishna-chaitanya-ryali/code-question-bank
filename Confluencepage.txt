# ---------------------------------------------------------
# CLEAR OLD ACCEPTED STATE
# ---------------------------------------------------------

if len(rejected_fields) == 9:

    sql_deleteData(
        398,
        [
            cascaded_metric_detail_id,
            rmm_id,
            "Accepted"
        ]
    )



DELETE FROM METRIC_CASCADE_REVIEW_STATE
WHERE CASCADED_METRIC_DETAIL_ID = ?
AND RMM_ID = ?
AND REVIEW_TYPE = ?