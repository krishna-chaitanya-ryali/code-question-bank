SELECT TARGET_METRIC_DETAIL_ID
FROM METRIC_CASCADE_ACTIONS
WHERE
    METRIC_DETAIL_ID = :1
    AND CHILD_RMM_ID = :2
    AND STATUS = 'Accepted'
    AND IS_ACTIVE = 'Y'
FETCH FIRST 1 ROW ONLY


accepted_metric = sql_getData(
    922,
    [
        metric_id,
        child_rmm_id
    ]
)

if accepted_metric:
    
    accepted_metric_id = accepted_metric[0].get(
        "TARGET_METRIC_DETAIL_ID"
    )
    
    sql_insertData(
        921,
        "NA",
        [
            child_rmm_id,
            accepted_metric_id
        ]
    )




old_version = int(current_active[0].get("CASCADE_VERSION", 1))

cascade_version = old_version + 1

version_data = sql_getData(
    923,
    [
        metric_id,
        child_rmm_id
    ]
)

cascade_version = (
    int(version_data[0].get("CASCADE_VERSION", 0)) + 1
)



SELECT
    NVL(MAX(CASCADE_VERSION), 0) AS CASCADE_VERSION
FROM METRIC_CASCADE_ACTIONS
WHERE
    METRIC_DETAIL_ID = :1
    AND CHILD_RMM_ID = :2