SELECT COUNT(*) AS NULL_ENGAGEMENT_ID_COUNT
FROM ENGAGEMENT
WHERE ENGAGEMENT_ID IS NULL;

SELECT
    ENGAGEMENT_ID,
    COUNT(*) AS RECORD_COUNT
FROM ENGAGEMENT
GROUP BY ENGAGEMENT_ID
HAVING COUNT(*) > 1;