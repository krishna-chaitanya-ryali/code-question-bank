SELECT
    COUNT(*) AS TOTAL_ROWS,
    COUNT(DISTINCT ENGAGEMENT_ID) AS DISTINCT_ENGAGEMENTS,
    COUNT(DISTINCT APP_ID) AS DISTINCT_APPLICATIONS,
    COUNT(DISTINCT IMPORTANT_BUSINESS_SERVICE) AS DISTINCT_BUSINESS_SERVICES
FROM IMPORTANT_BUSINESS_SERVICE_DETAILS;