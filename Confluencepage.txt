SELECT
    METRIC_DETAIL_ID,
    PARENT_RMM_ID,
    CHILD_RMM_ID,
    ACTION_TYPE,
    CASCADE_VERSION,
    VISIBLE_AT_RMM_ID,
    IS_ACTIVE
FROM METRIC_CASCADE_LOCATIONS
WHERE METRIC_DETAIL_ID = 237335
ORDER BY CHILD_RMM_ID, CASCADE_VERSION;