elif (
    action_type == "CASCADE"
    and old_status in [
        "Accepted",
        "Rejected"
    ]
):


if action_type == "REMOVE":
    new_status = "Awaiting Acceptance"
else:
    accepted_metric = sql_getData(...)