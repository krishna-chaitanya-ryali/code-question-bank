ALTER TABLE METRIC_CASCADE_ACTIONS
ADD CASCADE_VERSION NUMBER;



cascade_version = selected_row["CASCADE_VERSION"]

sql_insertData(
    390,
    "ACTION_ID",
    [
        metric_detail_id,
        parent_rmm_id,
        "CASCADE",
        str(vUser),
        "Awaiting Acceptance",
        child_rmm_id,
        meet_instc_id,
        cascade_dt_str,
        cascade_version
    ]
)



ON MCA.METRIC_DETAIL_ID = RMDT.METRIC_DETAIL_ID

ON MCA.METRIC_DETAIL_ID = MCL.METRIC_DETAIL_ID
AND MCA.CASCADE_VERSION = MCL.CASCADE_VERSION
AND MCA.CHILD_RMM_ID = MCL.RMM_ID

