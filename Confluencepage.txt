"CASCADE_REQUEST_STATUS": derive_cascade_request_status(
    len(record.get("ACCEPTED_FIELDS") or []),
    len(record.get("REJECTED_FIELDS") or [])
),


"CASCADE_REQUEST_STATUS": derive_cascade_request_status(
    accepted_count,
    rejected_count
),