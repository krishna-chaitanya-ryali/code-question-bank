SELECT
    METRIC_DETAIL_ID,
    CHILD_RMM_ID,
    LOCATION_TYPE,
    CASCADE_VERSION,
    ACTION_TYPE,
    PARENT_RMM_ID
FROM METRIC_CASCADE_LOCATIONS
WHERE METRIC_DETAIL_ID = 237462
ORDER BY CASCADE_VERSION, CHILD_RMM_ID;
