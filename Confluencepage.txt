rows_updated = sql_insertData(371, "NA", [])

log_debug.debug(
    f"System revoke flag updated | rows_updated={rows_updated}",
    extra={"job": job_name, "year": year}
)


if rows_updated == 0:
    log_debug.debug(
        "System revoke job executed but no eligible records found",
        extra={"job": job_name, "year": year}
    )


log_debug.debug(
    "System revoke job completed",
    extra={
        "job": job_name,
        "year": year,
        "rows_updated": rows_updated
    }
)
