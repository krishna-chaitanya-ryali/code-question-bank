SELECT
    METRIC_DETAIL_ID,
    PARENT_RMM_ID,
    CHILD_RMM_ID,
    ACTION_TYPE,
    CASCADE_VERSION,
    IS_ACTIVE
FROM METRIC_CASCADE_LOCATIONS
WHERE METRIC_DETAIL_ID IN (237331,237464)
ORDER BY
    METRIC_DETAIL_ID,
    CHILD_RMM_ID,
    CASCADE_VERSION;