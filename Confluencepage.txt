Intl.DateTimeFormat().resolvedOptions().timeZone

Asia/Kolkata
America/New_York
Europe/London


{
  "rmm_id": 14,
  "instance_id": 123,
  "selectedMetrics": [101, 102],
  "timezone": "Asia/Kolkata"
}


from datetime import datetime

action_dt = datetime.utcnow()


import pytz

def convert_to_user_tz(dt, tz_name):
    if not dt:
        return None

    utc = pytz.utc
    user_tz = pytz.timezone(tz_name)

    utc_dt = utc.localize(dt)
    local_dt = utc_dt.astimezone(user_tz)

    return local_dt.strftime("%d-%b-%Y %I:%M %p %Z")



timezone = vInfo.get("timezone", "UTC")

"CASCADE_DT": convert_to_user_tz(
    record.get("CASCADE_DT"),
    timezone
)


