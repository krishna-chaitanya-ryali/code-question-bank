def delete_review_rows(cascaded_metric_detail_id):

    try:
        Db_Con = capdb()

        SqlQry = QueryString[389]['Qstring']

        Db_Con.del_data(
            SqlQry,
            [cascaded_metric_detail_id]
        )

        Db_Con.endconnection()

        return "Removed"

    except Exception as e:

        print("delete_review_rows")
        print(e)

        return "Error"


delete_review_rows(cascaded_metric_detail_id)