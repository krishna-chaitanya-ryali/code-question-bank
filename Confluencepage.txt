ALTER TABLE RAP_RMM_GROUP ADD RMM_LEVEL NUMBER;

Level
Meaning
0
Global
1
GBs / Regions
2
LOBs / Markets
3
Sub-levels

SELECT RMM_ID
FROM RAP_RMM_GROUP
WHERE PARENT_RMM_ID = :current_rmm_id
AND RMM_LEVEL = (
    SELECT RMM_LEVEL + 1 
    FROM RAP_RMM_GROUP 
    WHERE RMM_ID = :current_rmm_id
)
AND ACTIVE = 'Y';

AND RMM_LEVEL <= :max_allowed_level

if child_level > MAX_CASCADE_LEVEL:
    reject