SELECT DISTINCT LINEMANAGER_ID
FROM RECERTIFICATION_EMAIL_LIST r
WHERE r.MAIL_SENT_COUNT = 3
  AND r.LINEMANAGER_ID IS NOT NULL
  AND TRUNC(SYSDATE) >= DATE '2025-01-13'
  AND r.LINEMANAGER_ID IN ('LM_ID_1', 'LM_ID_2');
