cascaded_location_ids = data.get("CASCADED_LOCATIONS", "")

if isinstance(cascaded_location_ids, str):
    cascaded_location_ids = [
        int(x.strip()) for x in cascaded_location_ids.split(",") if x.strip()
    ]

values = []

for rmm_id in cascaded_location_ids:
    location_type = "MARKET"  # or your logic
    values.append((METRIC_DETAIL_ID, rmm_id, location_type, userid))

self.bulk_update_data(insert_query, values)