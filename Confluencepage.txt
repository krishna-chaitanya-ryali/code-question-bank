SELECT
    ACTION_ID,
    METRIC_DETAIL_ID,
    CHILD_RMM_ID,
    ACTION_TYPE,
    STATUS,
    IS_ACTIVE,
    ACTION_BY,
    ACTION_DT_STR,
    CASCADE_VERSION
FROM METRIC_CASCADE_ACTIONS
WHERE METRIC_DETAIL_ID = 237335
ORDER BY ACTION_ID;