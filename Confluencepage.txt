if isinstance(selected_child_locations, str):
    selected_child_locations = [
        x.strip()
        for x in selected_child_locations.split(",")
        if x.strip()
    ]


selected_locations = set(
    map(str, selected_child_locations)
)

result = [
    row
    for row in result
    if str(row.get("CHILD_RMM_ID")) in selected_locations
]



log_debug.debug(
    "After conversion : %s",
    selected_child_locations
)