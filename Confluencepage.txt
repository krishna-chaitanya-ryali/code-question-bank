Before clicking Cascade,
should same metric + same location be selectable again?

ALTER TABLE METRIC_CASCADE_LOCATIONS
ADD (
    CASCADE_STATUS VARCHAR2(50),
    IS_ACTIVE VARCHAR2(1) DEFAULT 'Y',
    PARENT_ACTION_ID NUMBER,
    CASCADE_VERSION NUMBER DEFAULT 1
);


SELECT
    MCA.ACTION_ID,
    MCA.STATUS,
    MCA.ACTION_DT
FROM
    METRIC_CASCADE_ACTIONS MCA
WHERE
    MCA.METRIC_DETAIL_ID = ?
    AND MCA.CHILD_RMM_ID = ?
ORDER BY
    MCA.ACTION_DT DESC
FETCH FIRST 1 ROWS ONLY



for rmm_id in to_insert:

latest_action = sql_getData(
    401,
    [
        metric_detail_id,
        rmm_id
    ]
)


UPDATE
    METRIC_CASCADE_LOCATIONS
SET
    IS_ACTIVE = 'N',
    CASCADE_STATUS = 'Revised Cascade',
    UPDT_DT = CURRENT_TIMESTAMP,
    UPDT_BY = ?
WHERE
    METRIC_DETAIL_ID = ?
    AND RMM_ID = ?
    AND IS_ACTIVE = 'Y'


INSERT INTO METRIC_CASCADE_LOCATIONS
(
    CASCADED_ID,
    METRIC_DETAIL_ID,
    RMM_ID,
    LOCATION_TYPE,
    CREAT_BY,
    CASCADE_STATUS,
    IS_ACTIVE,
    CASCADE_VERSION
)
VALUES
(
    ISEQ$$_92316.NEXTVAL,
    ?,
    ?,
    ?,
    ?,
    'Re-Cascaded',
    'Y',
    ?
)

SELECT
    NVL(MAX(CASCADE_VERSION), 0) + 1 AS NEXT_VERSION
FROM
    METRIC_CASCADE_LOCATIONS
WHERE
    METRIC_DETAIL_ID = ?
    AND RMM_ID = ?


