metric_detail_id = record.get(
    "METRIC_DETAIL_ID"
)

# ---------------------------------------------------------
# ACCEPTED COUNT
# ---------------------------------------------------------

accepted_rows = sql_getData(
    392,
    [
        metric_detail_id,
        rmm_id,
        "Accepted"
    ]
)

accepted_count = len(
    accepted_rows or []
)

# ---------------------------------------------------------
# REJECTED COUNT
# ---------------------------------------------------------

rejected_rows = sql_getData(
    396,
    [
        metric_detail_id,
        rmm_id
    ]
)

rejected_count = len(
    rejected_rows or []
)