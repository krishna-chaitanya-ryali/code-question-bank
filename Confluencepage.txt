metric_ids = vInfo.get("selectedMetrics", [])

# 🔥 Normalize everything
if isinstance(metric_ids, str):
    metric_ids = [metric_ids]

if isinstance(metric_ids, int):
    metric_ids = [metric_ids]

if not isinstance(metric_ids, list):
    return jsonify({"status": "error", "message": "Invalid selectedMetrics format"})


log_error.error(f"DEBUG metric_ids type: {type(metric_ids)} value: {metric_ids}")


