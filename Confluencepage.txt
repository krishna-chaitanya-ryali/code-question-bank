sql_insertData(
    938,
    "NA",
    [
        int(rmm_id),                  # New owner (217)
        cascaded_metric_detail_id,    # Parent metric detail id (e.g. 237335)
        previous_parent_rmm,          # Parent RMM (14)
        int(rmm_id)                   # Child RMM (217)
    ]
)