from datetime import datetime
import pytz

IST = pytz.timezone("Asia/Kolkata")

scheduler.add_job(
    system_revoke_job,
    trigger="date",
    run_date=IST.localize(datetime(2025, 1, 13, 17, 0)),
    id="system_revoke_job",
    replace_existing=True
)
