SELECT
    METRIC_DETAIL_ID,
    PARENT_RMM_ID,
    CHILD_RMM_ID,
    CASCADE_VERSION,
    ACTION_TYPE,
    STATUS,
    IS_ACTIVE,
    ACTION_DT_STR
FROM METRIC_CASCADE_ACTIONS
WHERE METRIC_DETAIL_ID = 237399
ORDER BY ACTION_DT;


