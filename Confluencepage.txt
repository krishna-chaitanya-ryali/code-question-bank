SELECT
    CONTRACT_REF_NUM,
    COUNT(*) AS RECORD_COUNT
FROM CONTRACT
GROUP BY CONTRACT_REF_NUM
HAVING COUNT(*) > 1;


SELECT
    CONTRACT_ID,
    COUNT(*) AS RECORD_COUNT
FROM CONTRACT
GROUP BY CONTRACT_ID
HAVING COUNT(*) > 1;