get_removed_metric = reqs.args.get(
    'get_removed_metric'
)

if get_removed_metric == "remove":

    details_list = sql_getData(
        915,
        [int(metric_id)]
    )

else:

    details_list = sql_getData(
        522,
        [int(metric_id)]
    )

