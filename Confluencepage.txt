"Ram, for the parameter/configuration file, do we have any existing standard in the project — YAML, JSON, or something else? If not, I'll use YAML because the file/sheet/target-table parameters will be easy to maintain."