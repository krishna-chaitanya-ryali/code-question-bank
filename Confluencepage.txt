Hi Prasad,

After analyzing the re-cascade workflow in detail, we would like to confirm the below functional behaviors before implementation, as these impact the overall parent-child cascade lifecycle and historical row handling.

1. When a metric is re-cascaded by Parent and the same metric is already present under Child → "Cascaded From Parent" section:

- Should the OLD row become "Revised Cascade" and greyed out?
- Should a NEW row be inserted with status as "Awaiting Acceptance"?

(Current understanding: YES)

2. When the same metric was already Accepted by Child and available under Child → "Cascaded To Child" section:

- On re-cascade from Parent, should the existing row under "Cascaded To Child" immediately become greyed out and Actioned By updated to "System"?

(Current understanding: YES)

3. During re-cascade flow, should NO new row be immediately created under Child → "Cascaded To Child" section?
   And should the new row appear there only AFTER Child re-accepts the revised metric successfully?

(Current understanding: YES)

4. Since the metric is already present in Child dashboard after first acceptance:

- During re-cascade, should Child user mandatorily select "Existing Metric" option and map to the already existing metric from dropdown instead of creating a new metric?

(Current understanding: YES)

5. If Parent re-cascades the same metric multiple times before Child acts:

- Should all previous pending rows become "Revised Cascade" and only the latest row remain actionable?

6. Should historical rows continue to remain visible in UI for audit/history purposes while being greyed out?

(Current understanding: YES)

Please confirm if the above understanding is aligned with business expectation so that we can proceed with implementation accordingly.