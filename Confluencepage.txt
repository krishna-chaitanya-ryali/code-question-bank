ALTER TABLE METRIC_CASCADE_ACTIONS 
ADD ACTION_DT_STR VARCHAR2(50);

current_time = vinfo.get("current_time")

sql_insertData(
    379,
    "ACTION_ID",
    [metric_id, parent_rmm_id, child_rmm_id, user_id, meet_instc_id, current_time]
)


ACTION_DT     -> SYSTIMESTAMP
ACTION_DT_STR -> :6

