def build_metric_assignment_response(vRecs):
    response = []

    for record in vRecs:
        response.append({
            "metric_detail_id": record.get("METRIC_DETAIL_ID"),
            "metric_name": record.get("METRIC_NAME"),
            "metric_name_cascade": record.get("METRIC_NAME_CASCADE"),
            "threshold_cascade": record.get("THRESHOLD_CASCADE"),
            "metric_applicability": record.get("METRIC_APPLICABILITY")
        })

    return response

SELECT 
    RMDT.METRIC_DETAIL_ID,
    RMDT.METRICS_DISPLAY AS METRIC_NAME,
    RMDT.METRIC_NAME_CASCADE,
    RMDT.THRESHOLD_CASCADE,
    RMDT.METRIC_APPLICABILITY
FROM MEET_INSTC MI
JOIN RAP ON MI.MEET_INSTC_ID = RAP.RAP_INSTANCE_ID
JOIN RAP_METRICS_DETAILS RMDT ON RAP.RAP_ID = RMDT.RAP_ID

WHERE MI.RMM_ID = :rmm_id
AND MI.MEET_INSTC_ID = :meet_instc_id
AND RMDT.METRIC_FLAG <> 'removed'

AND EXISTS (
    SELECT 1
    FROM METRIC_CASCADE_LOCATIONS MCL
    WHERE MCL.METRIC_DETAIL_ID = RMDT.METRIC_DETAIL_ID
    AND MCL.RMM_ID = :rmm_id   -- 🔥 KEY FILTER
    AND NVL(MCL.ACTIVE, 1) = 1 -- if you're using soft delete
)
