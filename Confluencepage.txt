CASE
    WHEN MCL.PARENT_RMM_ID = :2
    THEN 'CURRENT_LOCATION'

    WHEN MCL.CHILD_RMM_ID = :2
    THEN 'PARENT_LOCATION'

    ELSE NULL
END AS REMOVE_SOURCE_TYPE


"REMOVE_SOURCE_TYPE": record.get(
    "REMOVE_SOURCE_TYPE"
),

WHERE (
        MCL.PARENT_RMM_ID = :2
        OR
        MCL.CHILD_RMM_ID = :2
      )
  AND MI.MEET_INSTC_ID = :3
  AND RMDT.METRIC_FLAG = 'remove


"REMOVE_SOURCE_TYPE": record.get(
    "REMOVE_SOURCE_TYPE"
),