SELECT
    METRIC_DETAIL_ID,
    CHILD_RMM_ID,
    CASCADE_VERSION,
    ACTION_TYPE,
    ACTION_BY,
    ACTION_DT,
    ACTION_DT_STR,
    IS_ACTIVE
FROM METRIC_CASCADE_ACTIONS
WHERE METRIC_DETAIL_ID = 237335
ORDER BY ACTION_DT DESC;