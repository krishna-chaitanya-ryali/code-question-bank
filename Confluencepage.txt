ALTER TABLE METRIC_CASCADE_ACTIONS
ADD REVIEW_STATUS VARCHAR2(50);

ALTER TABLE METRIC_CASCADE_ACTIONS
ADD LAST_UPDATED_BY VARCHAR2(100);

ALTER TABLE METRIC_CASCADE_ACTIONS
ADD LAST_UPDATED_DT TIMESTAMP;

CREATE TABLE METRIC_CASCADE_REVIEW_DETAILS (

    REVIEW_ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,

    CASCADED_METRIC_DETAIL_ID NUMBER NOT NULL,

    FIELD_KEY VARCHAR2(100) NOT NULL,

    REMARKS VARCHAR2(2000),

    FILE_NAME VARCHAR2(500),

    FILE_PATH VARCHAR2(1000),

    CREATED_BY VARCHAR2(100),

    CREATED_DT TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


def get_cascade_review_status(rejected_fields, total_fields=9):

    rejected_count = len(rejected_fields)

    if rejected_count == 0:
        return "Accepted"

    elif rejected_count == total_fields:
        return "Rejected"

    return "In Progress"



def insert_rejected_review_details(
    cascaded_metric_detail_id,
    rejected_fields,
    remarks,
    file_name,
    file_path,
    user
):

    dbCon = rapdb()

    sql = """
        INSERT INTO METRIC_CASCADE_REVIEW_DETAILS (
            CASCADED_METRIC_DETAIL_ID,
            FIELD_KEY,
            REMARKS,
            FILE_NAME,
            FILE_PATH,
            CREATED_BY,
            CREATED_DT
        )
        VALUES (
            :1, :2, :3, :4, :5, :6, CURRENT_TIMESTAMP
        )
    """

    values = []

    for field in rejected_fields:

        values.append((
            cascaded_metric_detail_id,
            field,
            remarks,
            file_name,
            file_path,
            user
        ))

    dbCon.bulk_execute(sql, values)


file_name = uploaded_file.filename

file_path = os.path.join(
    NAS_DIRECTORY,
    str(cascaded_metric_detail_id),
    file_name
)




7. FINAL REJECTION API FLOW
Backend Steps
STEP 1
Receive payload:
Python
cascaded_metric_detail_id
rejectedFieldKeys
remarks
file
STEP 2
Store file in NAS
STEP 3
Calculate status
Python
status = get_cascade_review_status(rejected_fields)
STEP 4
Insert rejected rows
Python
insert_rejected_review_details(
    cascaded_metric_detail_id,
    rejected_fields,
    remarks,
    file_name,
    file_path,
    user
)
STEP 5
Update METRIC_CASCADE_ACTIONS
Python
sql = """
UPDATE METRIC_CASCADE_ACTIONS
SET
    STATUS = :1,
    REVIEW_STATUS = :2,
    LAST_UPDATED_BY = :3,
    LAST_UPDATED_DT = CURRENT_TIMESTAMP
WHERE METRIC_DETAIL_ID = :4
"""

values = [
    status,
    status,
    user,
    cascaded_metric_detail_id
]
✅ 8. FETCH REMARKS API
When user clicks blue “Link”.
Request
JSON
{
  "cascaded_metric_detail_id": 236681
}
Backend Query
SQL
SELECT
    FIELD_KEY,
    REMARKS,
    FILE_NAME,
    FILE_PATH,
    CREATED_BY,
    CREATED_DT
FROM METRIC_CASCADE_REVIEW_DETAILS
WHERE CASCADED_METRIC_DETAIL_ID = :1
ORDER BY CREATED_DT DESC
✅ 9. SAMPLE RESPONSE
JSON
{
  "status": "In Progress",
  "rejectedFields": [
    "PRIMARY_RISK_TYPE",
    "MASTER_METRIC",
    "FREQUENCY"
  ],
  "remarks": "Business rejected these fields",
  "fileName": "dispensation.xlsx",
  "filePath": "/nas/cascade/236681/dispensation.xlsx"
}
Frontend will render it.
Backend done.
✅ 10. IMPORTANT FINAL ARCHITECTURE
ACCEPT FLOW
Already handled by:
Add API
Edit API
No extra storage needed.
REJECT FLOW
Handled separately by:
rejection API
review details table
✅ 11. VERY IMPORTANT
Do NOT store accepted fields.
Your correction was correct.
Only rejected fields matter for:
audit
dispensation
remarks popup
rejection tracking
That is enough.
✅ 12. FINAL DESIGN RESULT
