936: {
    "Qstring": """
        SELECT
            NVL(MAX(CASCADE_VERSION), 0) + 1 AS CASCADE_VERSION
        FROM METRIC_CASCADE_LOCATIONS
        WHERE METRIC_DETAIL_ID = :1
          AND CHILD_RMM_ID = :2
          AND LOCATION_TYPE = :3
    """
},


version_data = sql_getData(
    936,
    [
        selected_metric_detail_id,
        child_rmm,
        location_type
    ]
)