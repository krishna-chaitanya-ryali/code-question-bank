SELECT
    THIRD_PARTY_ID,
    ENGAGEMENT_ID,
    CONTRACT_REF_NUM,
    COUNT(*) AS RECORD_COUNT
FROM GTP_ENG_CONTR_REL
GROUP BY
    THIRD_PARTY_ID,
    ENGAGEMENT_ID,
    CONTRACT_REF_NUM
HAVING COUNT(*) > 1;