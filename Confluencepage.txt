if dispensation_files:

    uploaded_file = (
        dispensation_files[0]
    )

    # -------------------------------------------------
    # HANDLE DICT
    # -------------------------------------------------

    if isinstance(uploaded_file, dict):

        file_name = (
            uploaded_file.get("name")
        )

    # -------------------------------------------------
    # HANDLE STRING
    # -------------------------------------------------

    elif isinstance(uploaded_file, str):

        file_name = uploaded_file

    # -------------------------------------------------
    # HANDLE TUPLE/LIST
    # -------------------------------------------------

    elif (
        isinstance(uploaded_file, tuple)
        or isinstance(uploaded_file, list)
    ):

        file_name = uploaded_file[0]

    # -------------------------------------------------
    # FILE PATH
    # -------------------------------------------------

    if file_name:

        file_path = (
            FOLDER_EXCEL
            + f"{cascaded_metric_detail_id}/"
            + file_name
        )