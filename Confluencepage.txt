Jira Update / Activity Details:
We need to refresh (downgrade) the RAPUSER schema from Production to UAT and DEV environments.
Steps to be performed:
Backup Existing Schemas
Take a full backup of the existing schemas:
RAPUSER in UAT
RAPUSER in DEV
Backup should be stored with appropriate naming convention, e.g.:
RAPUSER_BKP_<date>
Refresh from Production
Import/replace the Production RAPUSER schema data into:
UAT RAPUSER schema
DEV RAPUSER schema
Additional Schema Creation
Post refresh, create a new schema:
RAPUSER_ETL
Populate RAPUSER_ETL with the same data as Production RAPUSER schema
Summary:
Backup existing UAT & DEV schemas
Replace UAT & DEV RAPUSER with Production data
Create and populate RAPUSER_ETL with Production data