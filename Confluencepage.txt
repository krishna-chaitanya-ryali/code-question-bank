938: {
    "Qstring": """
        UPDATE METRIC_CASCADE_LOCATIONS
        SET VISIBLE_AT_RMM_ID = :1
        WHERE METRIC_DETAIL_ID = :2
          AND PARENT_RMM_ID = :3
          AND CHILD_RMM_ID = :4
          AND ACTION_TYPE = 'REMOVE'
          AND IS_ACTIVE = 'Y'
    """
},



for child_rmm_id in selected_rmm_ids:

    # Convert CASCADE -> REMOVE
    sql_insertData(
        913,
        "NA",
        [
            metric_detail_id,
            parent_rmm_id,
            child_rmm_id
        ]
    )

    # Initially visible only in current(parent) dashboard
    sql_insertData(
        938,
        "NA",
        [
            parent_rmm_id,
            metric_detail_id,
            parent_rmm_id,
            child_rmm_id
        ]
    )