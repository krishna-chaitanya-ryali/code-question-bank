UPDATE METRIC_CASCADE_ACTIONS
SET
    IS_ACTIVE = 'N',
    LAST_UPDATED_DT = CURRENT_TIMESTAMP
WHERE METRIC_DETAIL_ID = :1
  AND CHILD_RMM_ID = :2
  AND ACTION_TYPE = 'REMOVE'
  AND IS_ACTIVE = 'Y'


if action == "accept_removal_from_parent":

    selected_metric_detail_id = vInfo.get("selected_existing_metric_detail_id")
    rmm_id = vInfo.get("rmmid")

    # REMOVE METRIC FROM DASHBOARD
    sql_insertData(
        54,
        "NA",
        [
            current_time,
            selected_metric_detail_id
        ]
    )

    # DEACTIVATE REMOVE ACTION
    sql_insertData(
        917,
        "NA",
        [
            selected_metric_detail_id,
            rmm_id
        ]
    )

ALTER TABLE METRIC_CASCADE_DISPENSATION_DETAILS
ADD ACTION_TYPE VARCHAR2(50)


INSERT INTO METRIC_CASCADE_DISPENSATION_DETAILS
(
    DISPENSATION_ID,
    CASCADED_METRIC_DETAIL_ID,
    FIELD_KEY,
    REMARKS,
    ACTION_TYPE
)
VALUES
(
    DISPENSATION_ID_SEQ.NEXTVAL,
    :1,
    :2,
    :3,
    :4
)


elif action == "reject_removal_from_parent_with_rejection_remarks":

    selected_metric_detail_id = vInfo.get(
        "selected_existing_metric_detail_id"
    )

    rmm_id = vInfo.get("rmmid")

    remarks = vInfo.get("remarks")

    # STORE REJECTION REMARKS
    sql_insertData(
        919,
        "NA",
        [
            selected_metric_detail_id,
            "REMOVE_REJECTION_REMARKS",
            remarks,
            "REMOVE"
        ]
    )

    # DEACTIVATE REMOVE ACTION
    sql_insertData(
        917,
        "NA",
        [
            selected_metric_detail_id,
            rmm_id
        ]
    )


