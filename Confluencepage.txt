SELECT
    METRIC_DETAIL_ID,
    STATUS,
    ACTION_DT,
    LAST_UPDATED_DT,
    ACTION_DT_STR,
    NVL(
        LAST_UPDATED_DT,
        ACTION_DT
    ) AS SORT_DATE
FROM METRIC_CASCADE_ACTIONS
WHERE CHILD_RMM_ID = 2408
ORDER BY
    NVL(
        LAST_UPDATED_DT,
        ACTION_DT
    ) DESC;