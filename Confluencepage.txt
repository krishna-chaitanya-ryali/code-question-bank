CASE
    WHEN EXISTS (
        SELECT 1
        FROM METRIC_CASCADE_ACTIONS MCA3
        WHERE MCA3.TARGET_METRIC_DETAIL_ID = RMDT.METRIC_DETAIL_ID
          AND MCA3.CHILD_RMM_ID = :2
          AND MCA3.STATUS = 'Accepted'
    )
    THEN 'GLOBAL'
    ELSE 'LOCAL'
END AS METRIC_SOURCE_TYPE