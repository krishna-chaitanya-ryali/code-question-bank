CASE
    WHEN MCL.PARENT_RMM_ID = :1
    THEN NVL(MCL.IS_ACTIVE, 'N')

    WHEN MCL.CHILD_RMM_ID = :2
    THEN NVL(MCA.IS_ACTIVE, 'N')

    ELSE NVL(MCL.IS_ACTIVE, MCA.IS_ACTIVE)
END AS CASCADE_LIFECYCLE_STATUS