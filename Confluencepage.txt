for child_rmm_id in selected_rmm_ids:

    rec = sql_insertData(
        913,
        "NA",
        [
            metric_detail_id,
            parent_rmm_id,
            child_rmm_id
        ]
    )

    # If 913 updated 0 rows (no existing CASCADE row),
    # then insert a brand-new REMOVE row.